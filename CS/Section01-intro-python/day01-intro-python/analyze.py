from util import count_words, count_letters

text = (
    "A row of text"
    "Anoter row with a lot of text"
    "One more row with some text"
    "Even more text on a new row"
    )

print(count_words(text))
print(count_letters(text))